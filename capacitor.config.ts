import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'FoodAndRecipes',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
